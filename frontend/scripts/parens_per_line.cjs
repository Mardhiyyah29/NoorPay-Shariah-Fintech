const fs=require('fs');const lines=fs.readFileSync('frontend/src/App.jsx','utf8').split(/\r?\n/);let cum=0;let inS=false,inD=false,inB=false,esc=false;for(let i=560;i<=640;i++){const line=lines[i-1]||'';for(const ch of line){if(esc){esc=false;continue;} if(ch==='\\'){esc=true;continue;} if(inS){ if(ch==="'") inS=false; continue;} if(inD){ if(ch==='"') inD=false; continue;} if(inB){ if(ch==='`') inB=false; continue;} if(ch==="'"){inS=true;continue;} if(ch==='"'){inD=true;continue;} if(ch==='`'){inB=true;continue;} if(ch==='(') cum++; if(ch===')') cum--; } console.log(i, 'cum', cum, '=>', line.trim()); if(cum<0){console.log('NEG',i);break;} }console.log('FINAL',cum);