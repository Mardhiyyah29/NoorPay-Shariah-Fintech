const fs=require('fs');const s=fs.readFileSync('frontend/src/App.jsx','utf8');const lines=s.split(/\r?\n/);let inS=false,inD=false,inB=false,esc=false;for(let i=0;i<lines.length;i++){const line=lines[i];for(let j=0;j<line.length;j++){const ch=line[j];if(esc){esc=false;continue;} if(ch==='\\'){esc=true;continue;} if(inS){ if(ch==="'") inS=false; continue;} if(inD){ if(ch==='"') inD=false; continue;} if(inB){ if(ch==='`') inB=false; continue;} if(ch==="'"){inS=true;console.log('enter single at',i+1,j+1); } if(ch==='"'){inD=true;console.log('enter double at',i+1,j+1);} if(ch==='`'){inB=true;console.log('enter back at',i+1,j+1);} } }
console.log('final states', {inS,inD,inB});
